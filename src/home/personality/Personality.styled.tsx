import styled from '@emotion/styled';

const StyledHomePersonality = styled.section`
  .personality-content {
    padding: 40px 35px;
    background: rgba(0,0,0,0.2);
    border-radius: 12px;

    ul {
      margin: 0;
      line-height: 2;
    }
  }
`;

export default StyledHomePersonality;